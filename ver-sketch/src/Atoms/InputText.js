const InputText = ({ placeholder }) => {
  return (
    <input
      type="text"
      placeholder={placeholder}
      className="w-full border border-gray-400 rounded-none text-sm"
    />
  )
}

export default InputText
